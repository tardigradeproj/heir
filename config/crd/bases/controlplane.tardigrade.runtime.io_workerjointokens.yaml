---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: workerjointokens.controlplane.tardigrade.runtime.io
spec:
  group: controlplane.tardigrade.runtime.io
  names:
    kind: WorkerJoinToken
    listKind: WorkerJoinTokenList
    plural: workerjointokens
    singular: workerjointoken
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkerJoinToken is the Schema for the workerjointokens API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkerJoinToken
            properties:
              runtimeRef:
                description: |-
                  RuntimeRef names the Runtime (tenant control plane) this token grants access
                  to join. Must exist in the same namespace as this WorkerJoinToken. Immutable
                  after creation: retargeting a token at a different Runtime is a different
                  resource, not an update to this one — delete and recreate instead.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: runtimeRef is immutable
                  rule: self == oldSelf
              ttl:
                default: 3h
                description: |-
                  TTL is how long a minted token remains valid on the tenant cluster, starting
                  from the moment it is issued. Immutable after creation: this resource is
                  intentionally one-shot, not auto-renewing, so the only way to mint a token with
                  a different TTL is to delete and recreate the object.
                type: string
                x-kubernetes-validations:
                - message: ttl is immutable
                  rule: self == oldSelf
            required:
            - runtimeRef
            type: object
          status:
            description: status defines the observed state of WorkerJoinToken
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the WorkerJoinToken resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types are:
                    Ready    — a non-expired token and kubeconfig Secret are available.
                    Degraded — the controller could not reach the tenant cluster or mint a token.

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              expiresAt:
                description: ExpiresAt is when the currently issued token expires
                  on the tenant cluster.
                format: date-time
                type: string
              secretRef:
                description: |-
                  SecretRef names the Secret, in this object's namespace, holding the
                  base64-encoded bootstrap kubeconfig under key "kubeconfig". Owned by this
                  WorkerJoinToken; garbage-collected alongside it.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              tokenID:
                description: |-
                  TokenID is the public identifier of the currently issued bootstrap token (the
                  secret half is never surfaced on the object itself, only inside SecretRef).
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
